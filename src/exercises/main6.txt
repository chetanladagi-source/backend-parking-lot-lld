import { ParkingFloor } from "./modles/ParkingFloor";
import { ParkingSlot } from "./modles/ParkingSlot";
import { ParkingSlotType } from "./enums/ParkingSlotType";
import { CarCreator } from "./factories/CarCreator";

const floor = new ParkingFloor(1);

const creator = new CarCreator();

floor.addSlot(
    new ParkingSlot("A1", ParkingSlotType.CAR)
);

floor.addSlot(
    new ParkingSlot("A2", ParkingSlotType.CAR)
);

floor.addSlot(
    new ParkingSlot("A3", ParkingSlotType.CAR)
);

const slot = floor.getAvailableSlot();

const car = creator.createVehicle(
    "KA01AB1234",
    "White"
);

slot?.parkVehicle(car);

const nextSlot = floor.getAvailableSlot();

console.log(nextSlot?.getSlotNumber());